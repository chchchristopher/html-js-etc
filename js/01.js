let message;

message = 'Hello'; // storing string in message variable, yep
alert(message);

let fruit = 'Apple', cost = 8, fresh = true; // can do one-line, but better to separate the lines probably

//alert(fruit);
fruit = 'Orange';
alert(fruit);
